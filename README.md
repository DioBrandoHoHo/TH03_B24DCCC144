# Product Manager (React + TypeScript)
Ứng dụng quản lý danh sách sản phẩm với CRUD, tìm kiếm, lọc và phân trang.

## Chạy dự án
1. Cài npm/yarn
2. `npm install`
3. `npm run dev`

Dự án dùng Vite + React + TypeScript.
